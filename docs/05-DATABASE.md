# Տվյալների բազա

- **ORM.** Prisma — `prisma/schema.prisma`
- **Միգրացիաներ.** `prisma/migrations/`
- **Հոսթինգ.** Neon PostgreSQL (տե՛ս `reference/platforms/02-NEON.md`)
- **Կապի տող.** `DATABASE_URL`, `DIRECT_URL` — `.env.example`

Մանրամասն նկարագրություններ (եթե կան). `Documents/` — որոնում `db`, `ENVIRONMENT`։
